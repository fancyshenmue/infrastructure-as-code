---
- hosts: '{{ hn }}'
  gather_facts: no
  roles:
    - role: "{{ (playbook_dir + '/crontab_conf') | replace('playbook', 'roles') }}"
      become: True
      become_user: root
      become_method: sudo
